{
  "name": "@repo/contracts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "hardhat compile",
    "typechain": "hardhat typechain",
    "deploy": "hardhat compile && hardhat typechain && hardhat run scripts/deploy.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "@typechain/hardhat": "^8.0.0",
    "hardhat": "^2.23.0",
    "typechain": "^8.3.0"
  }
}

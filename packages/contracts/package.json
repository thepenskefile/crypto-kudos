{
  "name": "@repo/contracts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network localhost",
    "clean": "rm -rf .turbo node_modules artifacts cache"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "@repo/typescript-config": "*",
    "abitype": "^0.9.8",
    "hardhat": "^2.19.4",
    "prettier": "^3.2.5",
    "typescript": "5.8.2"
  }
}
